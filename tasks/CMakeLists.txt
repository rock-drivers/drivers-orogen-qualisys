# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(qualisysTaskLib)
ADD_LIBRARY(${QUALISYS_TASKLIB_NAME} SHARED 
    ${QUALISYS_TASKLIB_SOURCES})

add_dependencies(${QUALISYS_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${QUALISYS_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QUALISYS_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${QUALISYS_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${QUALISYS_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${QUALISYS_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${QUALISYS_TASKLIB_HEADERS}
    DESTINATION include/orocos/qualisys)

